import path from 'path';
import {defineConfig} from 'vite';

export default defineConfig(() => {
  return {
    build: {
      rollupOptions: {
        input: {
          main: path.resolve(__dirname, 'index.html'),
          garage: path.resolve(__dirname, 'garage.html'),
          level1: path.resolve(__dirname, 'level1.html'),
          level2: path.resolve(__dirname, 'level2.html'),
          level3: path.resolve(__dirname, 'level3.html'),
          complete: path.resolve(__dirname, 'complete.html'),
        },
      },
    },
    server: {
      // HMR is disabled in AI Studio via DISABLE_HMR env var.
      // Do not modify—file watching is disabled to prevent flickering during agent edits.
      hmr: process.env.DISABLE_HMR !== 'true',
      // Disable file watching when DISABLE_HMR is true to save CPU during agent edits.
      watch: process.env.DISABLE_HMR === 'true' ? null : {},
    },
  };
});

